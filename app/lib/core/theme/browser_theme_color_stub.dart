/// Non-web stub: there is no browser `theme-color` meta to update, so this is a
/// no-op. The native platforms handle the status bar via SystemUiOverlayStyle.
void updateThemeColorMeta(String hex) {}
